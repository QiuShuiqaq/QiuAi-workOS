# Role Module

RoleTemplate, RoleInstance, role configuration, role lifecycle, and KPI rules.

First-version delivery uses `MockPlatformStore` for install/list/detail APIs so the frontend can complete the Role -> Task loop without a local database.
