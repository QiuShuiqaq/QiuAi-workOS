# Workspace Module

Personal Workspace and Enterprise Workspace lifecycle.

Current local development mode uses mock seed data so frontend and backend work can continue without a local PostgreSQL instance.
